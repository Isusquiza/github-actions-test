name: ENV Variables

on: push

env: 
  WF_ENV: ${{ secrets.WF_ENV}}

jobs: 
  log-env:
    runs-on: ubuntu-latest
    env: 
      JOB_ENV: Availabkle to all steps in log-env job
    steps:
      - name: Log Env Variables
        env: 
          STEP_ENV: Available for this step
        run: |
          echo "WF_ENV: ${WF_ENV}"
          echo "JOB_ENV: ${JOB_ENV}"


